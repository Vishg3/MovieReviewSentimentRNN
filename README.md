# MovieReviewSentimentRNN

Streamlit app that performs sentiment analysis based on a user entered movie review and classifies it as positive or negative. Uses a Simple RNN trained on the IMDb dataset.
